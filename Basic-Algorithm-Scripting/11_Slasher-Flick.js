// Return the remaining elements of an array after chopping off n elements from the head.

// The head means the beginning of the array, or the zeroth index.

const slasher = (str, num) => {
    return `${str} {num}`;
}
slasher("Internet is down", 3)